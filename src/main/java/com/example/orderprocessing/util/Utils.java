package com.example.orderprocessing.util;

import java.util.UUID;

public class Utils {

    // generate a UUID
    public static String generateUUID() {
        return UUID.randomUUID().toString();
    }
}
